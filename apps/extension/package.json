{
  "name": "@learn-assistant/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit && node build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "0.21.5",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
